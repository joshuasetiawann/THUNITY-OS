# THUOS — GRUB boot configuration (used when building a bootable ISO).
set timeout=3
set default=0

menuentry "THUOS 0.2.0 (Boot Seed)" {
    multiboot /boot/kernel.elf
    boot
}

menuentry "THUOS 0.2.0 (serial debug)" {
    multiboot /boot/kernel.elf
    boot
}
